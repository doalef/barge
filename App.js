import React, { Component } from 'react';
import {View} from 'react-native';
import { Root } from './src/ui/navigation/router';

export default class App extends Component {
  render() {
    return (
      <Root />
    );
  }
}
